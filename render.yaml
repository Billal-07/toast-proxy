services:
  - type: web
    name: toast-proxy
    env: python
    region: oregon  # US region
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT